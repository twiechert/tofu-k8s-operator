apiVersion: apps/v1
kind: Deployment
metadata:
  name: tofu-k8s-operator
  namespace: tofu-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tofu-k8s-operator
  template:
    metadata:
      labels:
        app: tofu-k8s-operator
    spec:
      serviceAccountName: tofu-k8s-operator
      containers:
        - name: manager
          image: tofu-k8s-operator:dev
          imagePullPolicy: IfNotPresent
          args:
            - --metrics-bind-address=:8080
            - --health-probe-bind-address=:8081
          ports:
            - containerPort: 8080
              name: metrics
            - containerPort: 8081
              name: health
